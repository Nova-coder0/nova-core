{
  "name": "nova-core",
  "version": "0.1.0",
  "private": true,
  "description": "NOVA token contracts, transparency docs, and launch infrastructure.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "check:deploy": "hardhat run scripts/check-deploy-config.js",
    "deploy:bsc-testnet": "hardhat run scripts/deploy-v2.js --network bscTestnet",
    "deploy:bsc": "hardhat run scripts/deploy-v2.js --network bsc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@openzeppelin/contracts": "5.0.2",
    "chai": "^4.5.0",
    "ethers": "^6.13.5",
    "hardhat": "^2.22.19"
  }
}
